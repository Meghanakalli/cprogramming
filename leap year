#include<stdio.h>
#include<conio.h>
int main()
{
int n;
printf("\nEnter a year:");
scanf("%d",&n);
if(n%4==0)
 {
 printf("\nThe entered year is an leap year");
 }
else
 {
 printf("\nThe entered year is not an leap year");
 }
 
}
